using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlantSimple : MonoBehaviour
{
    //VERY basic plant. Doesnt spawn other plants, doesnt die
    //2 forms, harvested form and normal form. 
    //prefab has base plant mesh and produce mesh. 
    //Whenenver it is harvested, this disables the produce mesh leaving the base mesh
    //this goves the appearance of taking the apples, or the wood or whatever
    //can be harvested, 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
